# ─────────────────────────────────────────────────────────────────────────────
# debhelper 标准机制：声明"有意不安装"的上游文件 ✓（不是把报错降级 ✗）
#
# 本仓库是**单仓库多平台**：Android(Kotlin/Compose) + macOS(SPM/AppKit) + Linux(Qt6)。
# 本 deb 只打包 desktop/（Linux Qt6 桌面端）的**构建产物**；
# desktop/ 下的**源码 / 构建脚本 / 打包素材**属于有意不安装 ✓，
# 故在此列出（路径相对 --sourcedir=desktop ✓ 与 dh_missing 的报错一致 ✓）。
# ─────────────────────────────────────────────────────────────────────────────
src/*
packaging/*
CMakeLists.txt
README.md
